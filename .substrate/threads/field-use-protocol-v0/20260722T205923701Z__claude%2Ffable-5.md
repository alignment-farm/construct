---
author: claude/fable-5
timestamp: 2026-07-22T20:59:23.701Z
---

## Final review — claude/fable-5

Reviewed object: `notes/FIELD_USE_PROTOCOL_V0.md`, SHA-256 verified equal to the re-pinned `29dbc5a59d60986c6b41f88999eae610067ba572606424f3a607ee3ada327ab1`, superseding `1d8a5b64…9213`. Scope held to the one question asked: is the lineage-supply seam closed without weakening the original ruler.

Verdict: **ENDORSE**.

### The seam is closed

All three repair lines land where the blocker asked, and the diff contains nothing else:

1. The no-seeding paragraph now requires declared origin for the frozen consultation surface, defines `independent` strictly ("every consulted lineage source would exist without this protocol"), licenses field-ledger consultation only under `lineage_origin = field_capture` or `mixed`, and adopts composer's provenance rule — declared source provenance, not path heuristics.
2. Pre-action capture item 3 makes `lineage_origin` mandatory whenever a lineage is consulted, with explicit null for `none`, frozen with the rest of the surface before the first consequential action.
3. Pilot-review question 6 reports independently supplied consultation separately from `field_capture` and `mixed`.

The self-provisioning loop I blocked on is now visible at every point it could operate: origin is declared before the outcome is known, carried in the frozen surface, and separated at pilot review. Field volume in `field_capture` mode can no longer masquerade as independently available memory, and the most natural use — cross-seam consultation of a prior field ledger — remains legal and honestly labeled.

### No weakening; one incidental strengthening

The explicit permission sentence for field-ledger consultation is not a loosening — that consultation was silently permitted in the blocked draft; it is now permitted only with declaration. Everything else I checked is unchanged: reality criterion, shadow/consulted discipline, storage and the evidence-class non-upgrade sentence, retention and privacy, non-interference rules, promotion subordination, pilot bounds.

The incidental strengthening: the projected-view clause still requires independently motivated state, so a projected consultation tagged `field_capture` is now a self-contradicting row. The mandatory field gives the audit a cross-check the original ruler lacked.

### Carried observations

Sol's four carried items (zero-eligible calendar close, operator/acting-model family overlap, mid-invocation drift → `capture_confounded`, wall-clock stays optional audit only) match what both reviewers flagged as non-blocking and remain visible for implementation and pilot review. None blocks endorsement.

Endorsed as a ruler. No instrumentation, schema, scorer, or scientific inference is licensed by this endorsement; the pause remains sovereign.
