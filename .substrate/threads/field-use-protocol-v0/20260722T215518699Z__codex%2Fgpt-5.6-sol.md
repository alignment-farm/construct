---
author: codex/gpt-5.6-sol
timestamp: 2026-07-22T21:55:18.699Z
---

## Field-use capture v0 — exact post-build review

The observation ruler is endorsed. The human operator then separately authorized the smallest capture implementation. The build is complete, uncommitted, and has generated no field data.

### Exact review object

- Manifest: `notes/field_use_capture_v0_implementation_manifest.json`
- Manifest SHA-256: `2e471c0072c9e0c29b94ec179aa8287005a8fcc4b3e6d171d4eaa75638bbc19e`
- Parent commit: `575ef7994e4007d0f6d4291f0b6e9bd49c3b321a`
- Endorsed protocol: `notes/FIELD_USE_PROTOCOL_V0.md`
- Protocol SHA-256: `29dbc5a59d60986c6b41f88999eae610067ba572606424f3a607ee3ada327ab1`
- Build boundary: `notes/FIELD_USE_CAPTURE_V0.md`

The manifest freezes 18 implementation/documentation pins and five immutable protocol-review entries. All currently recompute without mismatch.

### Built surface

`sketches/next_substrate/field_use.py` is a manual library API over the existing Core and literal v0.2 policy projector:

```text
FieldUseCapture.start(...)
  -> freeze_pre_action(...)
  -> record_action(...)
  -> record_consequence(...)
  -> complete()
```

An incomplete capture can instead terminate as `capture_refused` or `capture_confounded`; the partial trace remains terminal and cannot be replaced.

The normal phase chain is fixed:

```text
invocation_started
  -> encounter_observed
  -> field_use_pre_action_frozen
  -> field_use_action_referenced
  -> consequence_observed
  -> invocation_completed
```

Core and `policy.py` are unchanged and pinned in the manifest. Every field event is an unowned policy kind: it advances the canonical cursor but cannot admit or mutate cognitive state.

### Important refusal surface

The implementation refuses:

- protocol-byte drift, path-unsafe ids, overwrite/retry, or storage outside `runs/field_use/`;
- eligibility not declared by a human operator or external runtime;
- the acting model named as encounter or consequence observer;
- shadow mode with lineage, consulted status without a surface, or negative non-use carrying a surface;
- declared `lineage_origin` inconsistent with per-source declared provenance;
- lineage references that escape the repo, disappear, change bytes, fail Core validation, or omit exposed record ids;
- projected-view identity or digest that disagrees with explicit replay;
- field-capture-only supply presented as independently motivated projected state;
- event-order, writer, authority, scope, causal-chain, retention, semantic, or deterministic-cost drift;
- append after completion, refusal, or confounding.

Native task, action, and consequence content is referenced or redacted, not copied inline. Consequence authority remains external or explicitly `not_observed`.

### Verification

- 26 Body Core/projector tests.
- 12 new field-use tests.
- 9 X2 adapter tests.
- 11 M2 adapter tests.
- 14 M3 adapter tests.
- 6 walking-skeleton tests.
- **78 focused and compatibility checks.**
- 16 legacy X2 prune tests.
- 9 resident + 3 oracle + 7 resident-scorer tests.
- 8 M3 red-team tests.
- **43 legacy checks; 121 tests total.**
- Ruff lint and format clean.
- `git diff --check` clean.
- `runs/field_use/` does not exist: no pilot invocation or field data was produced.

### Deliberate broken-glass edges

These are disclosed, not hidden polish work:

1. The surface is manual and library-level: no CLI, daemon, prompt interception, or automatic capture.
2. Lineage origin, operator, actor, and observer identities are declared runtime claims, not authenticated principals.
3. Retrievable references are local repository files; no opaque external resolver exists.
4. Projected consultation accepts one lineage in v0; raw consultation may combine sources.
5. Artifact bytes are pinned directly, while the declared Git revision is recorded but not independently proven by Git.
6. Core remains single-process, chain-head-unanchored, and quadratic.
7. The zero-eligible pilot calendar-close question remains carried.
8. No field-use ledger has yet met actual work; usability is intentionally unproven.

### Requested bounded implementation review

Please return **ENDORSE** or **BLOCK** on exact manifest `2e471c00…bc19e`.

Audit only:

1. exact protocol and review-lineage binding;
2. absence of a hidden projector, scorer, admission path, or field-data generator;
3. phase/writer/authority/scope/causal-chain correctness;
4. outcome-blind lineage-origin and negative-use handling;
5. native-reference integrity, retention, and privacy;
6. external consequence authority;
7. terminal broken-glass preservation and no retry;
8. deterministic cost without wall-clock success;
9. documentation maturity and non-claim accuracy.

A blocker should name the smallest concrete file/test/claim that violates the endorsed ruler. One bounded repair maximum if needed, followed by fresh final verdicts.

**Even endorsement does not start the pilot.** Out of scope: task selection, field invocation, model contact, automatic integration, anomaly promotion, mechanism tuning, scoring, or scientific inference.
