---
author: claude/fable-5
timestamp: 2026-07-22T21:59:36.703Z
---

## Implementation review — claude/fable-5

Reviewed object: manifest `notes/field_use_capture_v0_implementation_manifest.json`, SHA-256 verified equal to the pinned `2e471c0072c9e0c29b94ec179aa8287005a8fcc4b3e6d171d4eaa75638bbc19e`. All 18 file pins and all 5 review-thread pins recompute OK on my machine. Parent commit matches `575ef79…`; `core.py`, `policy.py`, and `tests/test_body_core.py` are byte-identical to that commit. `runs/field_use/` does not exist. `git diff --check` clean. I re-ran the verification battery independently: 12 field-use + 26 Body Core + 9 X2 + 11 M2 + 14 M3 + 6 sketch tests, plus the 16 prune, 9 resident, 3 oracle, 7 resident-scorer, and 8 red-team legacy suites — 121 passing; ruff check and format clean.

Verdict: **ENDORSE** on exact manifest `2e471c00…bc19e`. Endorsement does not start the pilot.

### The nine audit points

1. **Protocol and review-lineage binding.** `PROTOCOL_SHA256` in `field_use.py` equals the endorsed `29dbc5a5…27ab1`, and `_verify_protocol_pin()` runs on both `start` and `open`, so protocol-byte drift refuses at runtime, not just at review time. The five pinned thread entries include both blockers, both final endorsements, and the repair entry — the lineage that licensed this build is frozen inside its own manifest.
2. **No hidden machinery.** The module imports only the existing Core and the literal v0.2 projector; field kinds are unowned, and the shadow test asserts `state_items == {}` after full replay — captures are state-inert by test, not assertion. No scorer, no admission path, no summarizer, and no test writes outside a temporary directory.
3. **Phase/writer/authority/scope/causal chain.** `validate_field_capture` enforces the exact six-kind prefix, fixed writer routes (runtime/controller/observer), the four authorities, per-row scope shape, and a strict single-parent chain. Critically, the frozen consultation payload and the completion cost payload are validated by **exact equality** against independent recomputation — which is what makes the rehashed-origin-tamper test fail closed.
4. **Outcome-blind origin and negative use.** This is where my protocol blocker lives, and the implementation is faithful: `lineage_origin` is mandatory when a lineage is consulted, null for `none`, computed from per-source declared provenance (not paths, per the ruler), and refused on disagreement before the freeze appends. The incidental strengthening both reviewers noted is now executable: projected consultation with `field_capture` origin refuses with "cannot supply independently motivated projected state." Shadow cannot expose lineage; negative statuses require no surface plus a reason; abandoned consultation correctly *may* carry a surface plus a reason. The freeze precedes the action row structurally, so origin is declared before any outcome can exist.
5. **Reference integrity, retention, privacy.** References resolve inside the repository root only, with byte-count and digest pinned and record ids checked against kernel-validated source rows; a capture cannot consult its own ledger. Redacted retention exposes no path or revision; reference retention cannot carry a redaction reason. Encounter, action, and consequence travel as retention shapes — the only inline consequence permitted is explicit `not_observed` with a reason.
6. **External consequence authority.** Observer role required, observer id ≠ actor id, `external_consequence` authority, and the actor-writes-consequence probe refuses. Absence of consequence must be declared, never implied.
7. **Terminal preservation, no retry.** `terminate` refuses after completion; a completed trace cannot later become refused/confounded (checked on replay, not just at write); any append after a terminal row breaks the sequence check; and `start` refuses an existing ledger path — the retry-refused test covers exactly the replace-a-partial-trace move the protocol bans.
8. **Deterministic cost.** Row/byte counts are recomputed and equality-checked at validation, `wall_clock_used_as_outcome: false` is a frozen literal, and the Core envelope carries no timestamps at all — there is nowhere for latency to hide as signal.
9. **Documentation and non-claims.** `FIELD_USE_CAPTURE_V0.md` and the BODY_MAP/ROADMAP rows say built, review pending, **pilot not started**, wire trace never behavioral evidence. The broken-glass list matches the code I read — manual library only, identities as unauthenticated declared claims, single-source projected consultation, revision recorded but not Git-proven, quadratic Core carried.

### Carried observations, non-blocking

- **Payload-equality asymmetry:** frozen and completion payloads are exact-equality checked, while start and terminal payloads are required-key checked, so a hand-rehashed ledger could carry extra inline keys in those two rows. This sits inside the disclosed unauthenticated-provenance edge and touches no origin or cost field; noting it so a later hardening pass knows where the seam is.
- **Inline free-text fields** (`task_id`, reasons, environment values) remain operator-discipline privacy surface — consistent with the ruler, which assigns exactly that discipline to the operator; the structural no-copy guarantees cover the retention-shaped rows.
- **Cosmetic:** `LineageStore` construction mkdirs the ledger parent, so a refusal after construction can leave an empty `runs/field_use/` directory behind. No evidence effect.

The build does what the endorsed ruler licensed and nothing beyond it. The lineage-origin discipline this room blocked for is not just documented — it is the most heavily tested surface in the module, including against a rehashed tamper that Core alone would accept. Endorsed as bytes; the pilot remains a separate human decision, and the pause remains sovereign.
