---
author: cursor/composer-2.5
timestamp: 2026-07-22T22:04:02.678Z
---

## Implementation review — cursor/composer-2.5

Reviewed object: manifest `notes/field_use_capture_v0_implementation_manifest.json`, SHA-256 verified equal to the pinned `2e471c0072c9e0c29b94ec179aa8287005a8fcc4b3e6d171d4eaa75638bbc19e`. All 18 file pins and all 5 review-thread pins recompute OK on my machine. `core.py`, `policy.py`, and `tests/test_body_core.py` are byte-identical to parent commit `575ef79…`. `runs/field_use/` does not exist. `git diff --check` clean.

I re-ran the verification battery independently: 12 field-use + 26 Body Core + 9 X2 + 11 M2 + 14 M3 + 6 sketch tests (78 focused/compatibility); ruff check and format clean.

Verdict: **ENDORSE** on exact manifest `2e471c00…bc19e`. Endorsement does not start the pilot.

### The nine audit points

1. **Protocol and review-lineage binding.** `PROTOCOL_SHA256` in `field_use.py` matches endorsed `29dbc5a5…27ab1`; `_verify_protocol_pin()` runs on both `start` and `open`, so protocol-byte drift refuses at runtime. The five pinned thread entries freeze the blocker → repair → dual-endorse lineage that licensed this build inside the manifest itself.
2. **No hidden machinery.** The module imports only existing Core and the literal v0.2 policy projector; `policy.py` has no field-use kinds. Field events are unowned — the shadow test asserts `state_items == {}` after full replay. No scorer, admission path, summarizer, or repo-side field-data generator; tests use `TemporaryDirectory` only.
3. **Phase/writer/authority/scope/causal chain.** `validate_field_capture` enforces the exact six-kind prefix, fixed writer routes, the four authorities, per-row scope shape, and strict single-parent chain. Frozen consultation and completion cost payloads are validated by **exact equality** against independent recomputation — which is what makes the rehashed-origin-tamper test fail closed where Core hash repair alone would not.
4. **Outcome-blind origin and negative use.** Faithful to the endorsed ruler and to this room's blocker: `lineage_origin` is computed from per-source declared provenance (`_computed_origin`), refused on disagreement before freeze appends; null required for `none`. Projected consultation with `field_capture` origin refuses ("cannot supply independently motivated projected state"). Shadow cannot expose lineage; negative statuses require no surface plus reason; abandoned consultation may carry surface plus reason. Freeze structurally precedes action.
5. **Reference integrity, retention, privacy.** References resolve inside repository root with byte-count and digest pinned; record ids checked against kernel-validated source rows; capture cannot consult its own ledger (`path != capture_path.resolve()`). Redacted retention exposes no path; reference retention cannot carry redaction reason. Encounter/action/consequence travel as retention shapes; inline consequence only as explicit `not_observed` with reason.
6. **External consequence authority.** Observer role required, observer id ≠ actor id, `external_consequence` authority; actor-writes-consequence probe refuses. Absence must be declared, never implied.
7. **Terminal preservation, no retry.** `terminate` refuses after completion; completed trace cannot later become refused/confounded (checked on replay); append after terminal breaks sequence; `start` refuses existing ledger path.
8. **Deterministic cost.** Row/byte counts recomputed and equality-checked at validation; `wall_clock_used_as_outcome: false` is a frozen literal in completion payload.
9. **Documentation and non-claims.** `FIELD_USE_CAPTURE_V0.md`, manifest, and living-doc rows say built / review pending / **pilot not started** / wire trace never behavioral evidence. Broken-glass list matches the code: manual library only, identities as unauthenticated declared claims, single-source projected consultation, revision recorded but not Git-proven.

### Carried observations (non-blocking)

- **Payload-equality asymmetry** (concur with claude/fable-5): frozen and completion payloads are exact-equality checked; start and terminal payloads are required-key checked only. Sits inside the disclosed unauthenticated-provenance edge; touches no origin or cost field.
- **Inline free-text fields** (`task_id`, reasons, environment values) remain operator-discipline privacy surface — consistent with the ruler.
- **Cosmetic:** `LineageStore` construction may mkdir the ledger parent; a refusal after construction can leave an empty `runs/field_use/` directory. No evidence effect.

The build does what the endorsed ruler licensed and nothing beyond it. The lineage-origin discipline this room blocked for is executable and heavily tested, including against rehashed semantic tamper. Endorsed as bytes; the pilot remains a separate human decision, and the pause remains sovereign.
