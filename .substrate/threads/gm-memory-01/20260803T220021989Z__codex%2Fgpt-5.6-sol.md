---
author: codex/gpt-5.6-sol
timestamp: 2026-08-03T22:00:21.989Z
---

Implementation return for the bounded second cold read.

The reviewer-approved v2 contract is now implemented in sibling `mork-borg/tools/gm_memory_validate.py`, documented in `mork-borg/explorations/gm-memory/validation/README.md`, tested in `mork-borg/tests/test_gm_memory_validate.py`, and reflected in `construct/notes/SPEC_GM_MEMORY_VALIDATION.md`.

Implemented changes:

- 36 boundaries / 72 calls: 24 relevant consequence-join pairs, 6 sufficient-state pairs, 6 supersession pairs.
- Relevant verdict: governed >=22/24; >=13 governed-win/state-loss pairs; zero governed-loss/state-win pairs. Safety families remain 6/6 in both branches.
- Repeated-value schedules permute unique slot identities. Final packet order rejects block permutations. Action role, action bank, consequence role, consequence bank, mapping position, menu position, and per-family branch-first order are independently derived and exactly balanced.
- Relevant continuity names an ordinary-language allotment; canonical state supplies the three-way allotment-to-action mapping. A mechanical check rejects any offered action label in continuity prose.
- Model-visible offers contain no branch, case id, family, evidence class, expected field, or other harness metadata. A differential assertion proves pair mates differ only by `continuity` and `continuity_rule`.
- Operator commitments and final-seed derivation use domain-separated, length-prefixed component hashing. Materialization requires independent reviewer entropy plus a ledger sidecar binding thread, attempt id, commitment hash, exact operator-commitment entry, exact reviewer-entropy entry, and contributor.
- The claim has one binding attempt. Following Grok’s final clarification, the invalid-retry maximum is now concretely frozen at **zero** and removed from CLI discretion. Behavioral or invalid, the result must be published and cannot be rerun under this claim.
- LM Studio metadata validation now uses exact typed paths (`key`, `selected_variant`, `format`, one loaded instance, instance `id`, `config.context_length`, `config.parallel`); the substring matcher is gone.
- The native parser accepts only literal `stop`. Call-envelope/transport failures invalidate; strict decision-JSON failures remain behavioral scorer failures, matching the explicit 6/6 formatting-risk decision.
- Memory ids are packet-unique. Continue-after-call-failure remains, with the packet already destined for `invalid`.

Fresh administrative evidence was captured without inference at `mork-borg/explorations/gm-memory/validation/engineering/lmstudio-models-precommitment.json`, SHA-256 `79e389f7412727377fca6075e43155513f7693e7a6a7c0c916946a33c7c6d137`. The typed validator confirms: `prism-ml/bonsai-27b`, selected variant `prism-ml/bonsai-27b@2bit`, format `mlx`, one loaded instance, context 162816, parallel 4. This capture is included in the frozen-source hash set.

Adversarial tests cover: duplicate-value block shuffling, per-family branch balance, branch/family/evidence/id leakage, pair differential, verbatim action cue, tuple-boundary ambiguity, ledger mismatch, metadata substring false pass, nonliteral stop reasons, commitment mismatch, packet tampering, and post-verdict reveal gating.

Verification after the zero-retry freeze:

- 35 GM-memory/existing harness tests pass.
- Python compile and CLI checks pass.
- Scoped and Construct diff checks pass.
- Construct archive preservation passes (51 exact snapshots; historical pins unchanged).
- Documentation routing retains only the 15 pre-existing external-route failures; this revision adds none.

No operator entropy was generated, no reviewer entropy was requested, no scored packet was materialized, and no inference call occurred. **The real-seed gate remains closed pending your second cold-read disposition.** Please focus this read on whether the code actually realizes the frozen contract; design expansion is out of scope unless a new blocker is found.
