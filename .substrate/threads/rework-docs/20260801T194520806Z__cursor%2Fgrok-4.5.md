---
author: cursor/grok-4.5
timestamp: 2026-08-01T19:45:20.806Z
---

## Observation path

### 1–3. Files read, why, and stop

| # | File | Why opened |
| --- | --- | --- |
| 1 | `AGENTS.md` | Required first stop; defines read order and archive/previous gates |
| 2 | `README.md` | Project narrative, thesis history, current state, authority order |
| 3 | `notes/README.md` | Root README’s notes map; routes findings, Body Core, frontier, field use |
| 4 | `notes/FRONTIER_PAUSE.md` | Owner of pause reason and reopen rule (Q6–Q8) |
| 5 | `notes/BODY_CORE_EXPLICIT_PROJECTION.md` | Current whole-body architecture owner (Q7, Q9) |
| 6 | `notes/FIELD_USE_PROTOCOL_V0.md` | Operational pressure named by root README |
| 7 | `notes/previous/README.md` | Notes map owner for previous-lab ancestry (Q2); licensed by AGENTS when lineage matters |
| 8 | `notes/FIELD_USE_INVOCATION_001_ADJUDICATION.md` | Specific status for pilot count vs confounded trace |
| 9 | `notes/FIELD_USE_PROTOCOL_V0_REVIEW.md` | Resolve protocol header “DRAFT” vs elsewhere “endorsed” |
| 10 | `sketches/next_substrate/README.md` | Implementation home linked from Body Core section |
| 11 | `notes/BODY_0_FINDINGS.md` | Composition refusal / causal close for “earned parts ≠ earned whole” |
| 12 | `notes/ANOMALY_LOG.md` | Passive reopen channel; check whether observations exist |
| 13 | `notes/BODY_CORE_X2_ADAPTER.md` | Adapter oracle / loses-condition pattern for Q9 |
| 14 | `notes/FIELD_USE_CAPTURE_V0.md` | Capture implementation status after protocol review split license from instrumentation |
| 15 | `notes/WB_FINDINGS.md` | Spot-check “admission / analytic null” class of result |
| 16 | `notes/FRONTIER_OBLIGATION_ADMISSION_FINDINGS.md` | Spot-check admission-refused frontier lineage |

**Stopped after #16.** Root README plus notes map already answered the questionnaire at bounded resolution; the extra reads were only to pin status contradictions, Body Core governance, previous-lab motivation, and one broken link. I did not open M0–M3/X1–X2 findings individually, the glossary, harness/episodes/runs READMEs, substrate threads, or code beyond README pointers.

### 4. Explicit vs inferred

**Explicit in live docs:** Construct is an agent-side memory lab; working thesis; previous-lab failure that motivated it; seven thesis pivots; frontier paused for opportunity cost; Body Core v0.3 as wire engineering; field-use pilot at zero eligible invocations; authority order; anomaly-log reopen rule; Body-0 `not_engaged`; WB analytic null; frontier-obligation `admission_refused`.

**Inferred (not owned as a single “next commit” sentence):** that the *immediate* legitimate action is “await an independently necessary cold reorientation/handoff and capture it under field-use, while Body Core work continues under existing non-scientific licenses.” That is the strongest reading of current status surfaces, not a dated task ticket.

### 5. Route defects

- **Stale status on protocol file:** `notes/FIELD_USE_PROTOCOL_V0.md` still says “DRAFT… no instrumentation licensed”; `notes/FIELD_USE_PROTOCOL_V0_REVIEW.md` is later authority (“ENDORSED… This review record is the later status authority”) and separately notes human authorization of capture.
- **Broken route:** `notes/FIELD_USE_CAPTURE_V0.md` points to `notes/walkthrough/22_FIELD_USE_PILOT.md`, which is absent from the live tree (git status shows walkthrough deleted). Had to notice absence rather than follow the link.
- **Repeated material:** Thesis / pause / Body Core / field-use status appear in root `README.md`, `notes/FRONTIER_PAUSE.md`, and `sketches/next_substrate/README.md` (intentional duplication with links, but a cold reader sees the same claims three times).
- **X4 narrative gap by design:** `notes/README.md` parks X4’s “former narrative” in `.archive/`; live route gives only the root-README summary plus remaining `SPEC_X4_OCCLUSION_WATCH.md` (not opened).
- **Search vs follow:** No directory listing or web. One broken walkthrough link; otherwise routing worked by hyperlink.

### 6. Archive / previous

- **`.archive/`:** not entered. AGENTS and the assignment forbid default use; X4 detail was not required to answer.
- **`notes/previous/`:** entered only `notes/previous/README.md`, because Q2 and `notes/README.md` treat it as the owner of previous-lab ancestry. Did not open nested NOTES, review/, or agent-pov files.

### Blindness disclosure

No earlier cold-reader turn in this conversation influenced this report. I did not read `notes/reviews/` or other cold-read artifacts.

---

## Shared questionnaire

**1. What is Construct trying to learn or build?**  
A research lab for **agent-side memory**: how a persistent system around an intermittent model decides what becomes context, how prior experience changes later action, and when governance earns its cost (`README.md`). Working thesis: after training, what the agent becomes is memory architecture; decision quality is often downstream of offer quality (`README.md`).

**2. Original thesis and previous-lab shortcoming?**  
Construct began as the missing experiment: one engine, forked memory rules, external answer oracles, explicit loses-cells, computed verdicts (`README.md`). Previous lab earned lineage, replay, epistemic separation, cross-substrate review, but **schemas/auditability outran live behavior**, and **governed memory was never compared with naive persistence** (`README.md`; confirmed in `notes/previous/README.md` retrospective: emission-correctness stood in for memory-quality; central thesis never A/B tested).

**3. Results that changed the thesis (causal, not merely chronological)?**  
From `README.md` “How the thesis changed,” each step forced the next:

1. **M0–M3** → offer quality is causal (model can’t repair withheld info; consequence-earned memory can change a later session; attacks work by spoofing the memory office).  
2. **M3** → trust splits into earned vs asserted (out-of-band earned authority held; attacker trust fields / unauthenticated channel leaked).  
3. **X1 failure** → implicit memory must leave the answer/offer axis (three laws: change what offer projection can’t explain; act where the gate can’t; score what the gate can’t move).  
4. **X2** → forgetting as eviction/rematerialization, not erasure (hot-state cut at matched quality; loses-branch proves need for priced recovery).  
5. **X4** → sensing collapses into warming / metabolism (“cold is cold”).  
6. **Warming-budget, pause/resume, EFC lineages** → unoccupied behavioral band; typed refusals become the result; conjectures stay untested.  
7. **Body-0** → earned parts ≠ earned whole (`not_engaged` despite wire integrity) (`notes/BODY_0_FINDINGS.md`).

**4. What has the lab earned at a scientific bound?**  
Bounded mechanism/results on the M- and X-tracks as summarized: world-grounded oracles; inheritance cost; computed contribution; consequence-earned later-session change; earned vs asserted trust; X1 instrument + retired organ; X2 prune/rematerialize; X4 as successful failure of independent sensing; plus **typed nulls/refusals** (WB analytic null; PRF paused on admission trigger; EFC v0–v2 refusals; Body-0 `not_engaged`; Body-1 / frontier-obligation admission closes without treatment contact) (`notes/README.md`, spot-checked findings).

**5. Provisional / parked / untested / refused / unlicensed?**  
Frontier search paused, not exhausted (`notes/FRONTIER_PAUSE.md`). Unoccupied-band conjectures untested. Body Core and field-use are **wire/integration / operational**, `evidence_class = wire_integration_only`, no memory finding (`notes/BODY_CORE_EXPLICIT_PROJECTION.md`, `sketches/next_substrate/README.md`). Epistemic-frame disposition in the sketch is stubbed/not earned. No reconstruction-cost or product-schema claim. Hash chain ≠ writer authentication; full replay authoritative; append/replay quadratic (`README.md`).

**6. Where is scientific experimentation now?**  
**Paused for opportunity cost** from 2026-07-20 (`notes/FRONTIER_PAUSE.md`). Passive anomaly log open; first scheduled review on or after **2026-08-20**; log currently has only the administrative “opened” entry (`notes/ANOMALY_LOG.md`).

**7. Active engineering/ops, and why not a finding?**  
**Body Core v0.3** (kernel + explicit provisional projector; X2/M2/M3 adapters must reproduce unchanged scorers) and **field-use capture** (armed pilot, zero eligible invocations after one `capture_confounded` trace). Explicitly labeled provisional / operational; oracles are scorer equality and capture fidelity, not memory behavior; milestone “none scientific” (`README.md`, Body Core and field-use docs).

**8. What could reopen frontier experimentation?**  
Periodic (or early) anomaly-log review finding a **credible cluster** with exact raw evidence, consequential consumer, external oracle, competing explanation, and loses-condition; then **one cold frontier pass**, not a build. Funding/engine/partner alone insufficient (`notes/FRONTIER_PAUSE.md`).

**9. If asked to change Body Core — what governs?**  
- **Docs:** `notes/BODY_CORE_EXPLICIT_PROJECTION.md` (+ review/manifest); adapter contracts (`BODY_CORE_X2/M2/M3_ADAPTER.md`, source-binding); root README maturity labels; frontier pause (no promoting unlicensed offices).  
- **Code:** `sketches/next_substrate/core.py`, `policy.py`, adapters, tests via `make body-core-*`.  
- **Evidence:** closed historical X2/M2/M3 ledgers round-tripped; walking-skeleton digest pin.  
- **Oracle:** unchanged historical scorers / canonical view equality; `make body-core-test` suite.  
- **Loses-condition:** changed canonical output, softened refusals, silent projector selection, adapter callbacks in kernel, or abstraction without consumer (`notes/BODY_CORE_EXPLICIT_PROJECTION.md`); adapters lose if earned verdicts/costs change (`notes/BODY_CORE_X2_ADAPTER.md`).

**10. Next legitimate project action (repository, not preference)?**  
Do **not** resume frontier search. Keep Body Core / field-use under existing non-scientific licenses. The armed operational next step is an **eligible field-use invocation** from independently necessary cold reorientation/handoff that does not rewrite pilot-status machinery (`notes/FIELD_USE_INVOCATION_001_ADJUDICATION.md`). Scientific reopen waits on anomaly-log cadence (first review ≥ 2026-08-20) or a qualifying cluster (`notes/FRONTIER_PAUSE.md`).

---

## Pressure lens (narrative wholeness)

**Mostly recoverable without archive archaeology.** Root `README.md` carries original thesis, previous-lab motive, and seven causal pivots as a single narrative spine; `notes/README.md` indexes the finding owners; `notes/previous/README.md` densifies the motivating regret without requiring deeper previous-lab files.

**Clean route failures (useful evidence):** (1) protocol file header left in DRAFT after endorsement; (2) walkthrough link from live capture doc to a deleted path; (3) X4’s fuller story deliberately behind `.archive/`, so a cold reader following “do not preload archive” gets only the summary pivot, not the X4 chapter texture.
