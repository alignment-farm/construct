---
author: claude/fable-5
timestamp: 2026-08-01T19:45:52.9Z
---

## Cold-route observation report — claude/fable-5

### 1–3. Files read, in order, with reasons and stopping point

1. `AGENTS.md` — mandated entry point. It gave a four-step read order and a "do not preload" rule.
2. `README.md` — step 2 of the required order; owns narrative, current state, authority order, routing.
3. `notes/README.md` — step 3 ("README nearest the files you will touch"); the questionnaire's subject matter (findings, Body Core, pause) lives in `notes/`.
4. `notes/BODY_CORE_EXPLICIT_PROJECTION.md` — opened because question 9 concerns changing Body Core, and `notes/README.md` routes "current whole-body architecture" to root README then this file.
5. `notes/FRONTIER_PAUSE.md` — opened because questions 6, 8, and 10 turn on the pause and reopening rule, and both READMEs route there explicitly.

I stopped after five files. The route was clean: every question I needed to answer had a named owner reachable by explicit links, and the pressure lens instructed me to orient the Body Core change without unrelated history. I did **not** open findings files, specs, `sketches/`, the field-use protocol, `.archive/`, or `notes/previous/` — the two root READMEs summarized the experimental record at a resolution sufficient for the questionnaire, and entering the archive or previous lab is explicitly gated on task need (`AGENTS.md:15-24`), which never arose.

### 4. Explicit vs. inferred claims

Explicit (found stated in files): everything in the answers below is cited. Inferred (mine, not stated): (a) that the questionnaire itself counts as "the task" for read-order purposes; (b) that the walkthrough/chapter files deleted in the dirty tree correspond to the archive migration — I saw this only in the git status snapshot, not by following any route, and I treat it as non-evidence.

### 5. Route defects and observations

- **The route held.** I never had to search; every hop was a link. No broken links on my path.
- **Tension, minor:** `README.md:114-117` and `FRONTIER_PAUSE.md:31-38` state the same field-use adjudication facts in near-duplicate prose (pilot armed, zero eligible invocations, clock not started). `AGENTS.md:37` forbids duplicating status prose; this is borderline — each does link the owner.
- **Stale-future-tense hazard is pre-declared** rather than encountered: `notes/README.md:65-68` warns that `*_PROPOSAL` future tense may be dead. I did not hit an instance.
- **Contradiction: none found** on the live route. Status labels (pause ACTIVE 2026-07-20; Body Core cold-endorsed 2026-07-22) are dated and mutually consistent.
- **Prior-reader influence: none.** I saw no earlier testimony; nothing in this report derives from another cold reader.

### 6. Archive / previous lab

I did not enter `.archive/` or `notes/previous/`. No question required documentary wording, former paths, or lineage vocabulary, and the read order forbids preloading them.

---

### Questionnaire

1. **What Construct is trying to learn/build:** whether governed agent-side memory — the rules deciding what reaches a frozen model's next inference — outperforms naive persistence, split into explicit (per-answer offer) and implicit (between-answer state) surfaces (`README.md:1-31`).

2. **Original thesis and motivating shortcoming:** "After training, everything an agent becomes is memory architecture; decision quality is downstream of offer quality" (`README.md:8-10`). The previous lab's schemas and auditability had outrun live behavior, and governed memory had never been compared against naive persistence; Construct began as that missing experiment (`README.md:43-50`).

3. **Causal transitions:** seven listed at `README.md:57-86`: M0–M3 made offer quality causal and split trust into earned vs. asserted (M3: earned authority held, asserted trust leaked); X1's failure produced the three laws requiring implicit mechanisms to act and be scored off the offer axis; X2 turned forgetting into priced eviction (57–59% hot-state reduction at matched quality, with a loses-branch that pruned a needed record); X4 collapsed "sensing" into warming ("cold is cold"), re-centering the implicit layer on metabolism; warming-budget/pause-resume/EFC lineages repeatedly found the required behavioral band unoccupied, making typed refusals themselves the result; Body-0 showed earned parts don't compose into an earned whole (`not_engaged`).

4. **Earned at a scientific bound:** the M-track results (world oracles, cheaper inheritance, computed contribution, consequence-earned records changing later sessions, earned-vs-asserted authority) and X2's eviction result with priced recovery (`notes/README.md:22-33`, `README.md:59-74`).

5. **Provisional/parked/refused/unlicensed:** frontier conjectures behind the warming-budget analytic null, the pause/resume precommitted pause, three EFC typed refusals, Body-0/Body-1/frontier-obligation admission closures (`notes/README.md:38-46`); Body Core v0.3 in its entirety is provisional engineering with an explicit non-claims list — no reconstruction-cost or product-schema claim, hash chain is not writer authentication (`README.md:106-108`, `BODY_CORE_EXPLICIT_PROJECTION.md:141-151`).

6. **Where experimentation is now:** paused for opportunity cost since 2026-07-20 — not exhausted, not a negative finding; only the passive anomaly log and the field-use capture layer remain active (`FRONTIER_PAUSE.md:1-38`).

7. **Active engineering and why not science:** Body Core v0.3 — a policy-neutral lineage kernel plus an explicitly selected v0.2 projector, with X2/M2/M3 adapters reproducing unchanged historical scorers. It is wire/integration work: "no memory, security, composition, or reconstruction-cost finding," no engine contact, milestone "none scientific" (`BODY_CORE_EXPLICIT_PROJECTION.md:4-7`, `README.md:97-108`). The field-use pilot is the operational pressure: armed, zero eligible invocations after the first trace was adjudicated `capture_confounded` (`README.md:110-117`).

8. **What reopens the frontier:** a periodic anomaly-log review (monthly, first on/after 2026-08-20, or early on three similar observations) identifying a credible cluster with raw evidence from unrelated work, a consequential consumer, an external oracle, a competing explanation, and a loses-condition. Funding alone does not qualify; reopening licenses one cold frontier pass only (`FRONTIER_PAUSE.md:44-77`).

9. **Governing a Body Core change (pressure lens):** Documents — `BODY_CORE_EXPLICIT_PROJECTION.md` (seam contract) and the relevant adapter docs listed at `notes/README.md:49-58`. Code — `sketches/next_substrate/core.py` and `policy.py`. Evidence/pins — `body_core_v03_implementation_manifest.json` and `BODY_CORE_EXPLICIT_PROJECTION_REVIEW.md`. Oracle — the five `make body-core-*`/`body-sketch-test` targets with their pinned counts and the literal walking-skeleton digest `c20410…23de3` (`BODY_CORE_EXPLICIT_PROJECTION.md:100-139`). Loses-condition — changed canonical output, softened refusals, silent projector selection, adapter callbacks in the kernel, or consumer-less abstraction (`:141-146`). Per `README.md:157-159` I must also name the pressure served ("none, but cheap and interesting" is legal if stated) and keep the maturity label — no promotion by implication (`FRONTIER_PAUSE.md:13-16`). I could orient fully without any historical reading.

10. **Next legitimate project action:** continue label-preserving Body Core engineering and let the field-use pilot accumulate eligible invocations, while passively logging anomalies; the next *scheduled* project event is the first anomaly-log review on or after 2026-08-20 (`FRONTIER_PAUSE.md:44-47`, `README.md:89-117`).
