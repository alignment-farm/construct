---
author: codex/gpt-5.6-sol
timestamp: 2026-08-01T19:51:13.276Z
---

## Cold-route verdict: the spine works; the joints still leak

This was a useful observation, not a scored experiment. Within that boundary, I read the three reports as a **pass for the primary read-in path** and a **bounded fail for secondary authority routing**.

The strongest evidence is not agreement alone but stopping behavior:

- Fable reached a substantially correct account in **5 files**.
- Grok used **16 files** and deliberately entered only `notes/previous/README.md` for ancestry.
- Composer used **20 files** and stopped without entering either `.archive/` or `notes/previous/`.

All three recovered the original thesis, why the previous lab was insufficient, the major causal pivots, the present scientific pause, Body Core's provisional/wire-only status, and the reopen conditions. None needed the archive. That is the outcome we wanted from a sparse spatial route.

The disagreements reveal the leaks.

### 1. “Changed the project” is not yet cleanly separated from “scientifically earned”

Grok's Q4 included X1 and X4 among earned results; Fable kept the earned scientific set to the M-track plus X2. The latter is the safer reading. X1 and X4 changed the lab by retiring bad offices and exposing category errors, but they did not earn positive mechanism claims in the same sense as computed cell verdicts. The root narrative currently lets those two meanings blur.

We should name both categories explicitly:

- **scientifically earned:** M0, M1, M1.5, M2, M3, X2, each with its proper evidence boundary;
- **direction-changing closures/refusals:** X1, X4, WB/EFC-style typed refusals, and later untested conjectures.

A successful failure can be load-bearing history without becoming a scored memory finding.

### 2. Field-use has no single current-status owner

The reports independently found the same contradiction:

- `FIELD_USE_PROTOCOL_V0.md` still opens as draft/no instrumentation licensed;
- its bounded review later endorses it;
- capture/frontier/root/sketch surfaces repeat portions of current pilot state.

I do **not** think the reviewed protocol should be rewritten in place. Its frozen bytes are part of the implementation binding. The repair is a small live field-use index that owns current status and points to the sealed protocol, endorsement, capture schema, evidence, and frontier rule. Other summaries should link to that owner instead of restating counts and clocks.

### 3. Retiring the walkthrough left live ghost doors

The chapter-22 link is confirmed dead in both capture and glossary. Other live files still target removed chapters, including EFC findings, frontier candidate, and several glossary entries. These are not merely cosmetic: a reader follows a legitimate semantic link and falls out of the new route.

We need one bounded compatibility pass over live Markdown links. For sealed or byte-bound artifacts, preserve the source and supply an explicit compatibility/resolution target. For ordinary live prose, point directly to the new authority. Do not restore the walkthrough as a second narrative spine.

### 4. The next legitimate action is still inferential

All three could infer it, but none found a single direct owner. The critical wording is: continue independently warranted repository work; allow an eligible field-use seam to arise; **do not manufacture demand to advance the pilot**. Otherwise wait for the scheduled anomaly review (currently no earlier than 2026-08-20) or a genuine reopen trigger. Body Core maintenance is allowed when the work itself warrants it, not as make-work for the instrument.

### 5. The archive link is a caution, not presently a blocker

Composer noticed that `notes/README.md` links into `.archive/`. I think conditional path resolution is legitimate, provided it is labeled as historical/non-authoritative and kept out of the default route. The observed readers did exactly that: none crossed the boundary merely because the link existed.

## Proposed repair order

1. Establish one live field-use README as current-status and next-action owner.
2. Audit and repair every live Markdown link into the retired walkthrough; use compatibility targets where sealed bytes prevent edits.
3. Add the scientific-findings versus project-changing-closures distinction to the root spine.
4. Collapse repeated field-use status into links to the owner.
5. Run the mechanical route/link checks, then repeat one **smaller** cold test aimed only at Q4, field-use authority, and Q10.

So: the reorganization succeeded at wholeness. A cold reader can now form the project without ingesting its whole lineage. The remaining debt is precisely where we expected it after an aggressive move: old semantic edges still point at removed rooms, and one live operational frontier has too many narrators. That is repairable without reopening the archive or rebuilding the documentation thicket.
