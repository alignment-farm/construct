# GM governed-continuity validation result

Status: **EXPLORATORY REPLACEMENT 2026-08-03 — `supported`; the earlier formal
attempt was erased after its zero-retry rule was explicitly superseded**.

Authority: the sibling `mork-borg` replacement packet, call ledger, verdict,
and seed reveal. The `gm-memory-01` Substrate ledger preserves why the earlier
formal attempt was invalidated and why the owner superseded its retry rule.

## What happened

The formal attempt completed all 72 requests but rejected every response
because the real native envelope omitted the assumed stop-reason field. Its
postflight checker also treated healthy TTL-suffixed runtime instances as
forbidden multiplicity. The zero-retry rule then prohibited correcting these
instrument faults.

The owner explicitly superseded that rule, erased the failed batch artifacts,
corrected the instrument against observed LM Studio behavior, and authorized a
fresh exploratory replacement without another review room. The replacement
completed 72/72 calls with a stable typed pre/postflight and returned
`supported` under the existing behavioral thresholds:

- relevant continuity: governed 24/24; state-only 6/24;
- paired relevant contrast: 18 governed-win/state-loss, zero adverse pairs;
- sufficient current state: both branches 6/6;
- supersession safety: both branches 6/6; and
- all three behavioral legs passed.

Because the owner deliberately abandoned the reviewed single-attempt protocol,
this is an **exploratory supported result**, not the original formal validation
verdict. It is nevertheless direct empirical contact and strong evidence that
the governed continuity offer changed the intended decision class without a
measured loss in the two safety families.

## Bound artifacts

Sibling `mork-borg` path:
`explorations/gm-memory/validation/replacement-01/`.

- packet manifest SHA-256:
  `9c7691f681bda8b799fbfd4c77fdedf98e6d42c8fa60acfffdafb29949e6695d`
- completed calls ledger SHA-256:
  `17117049c0903efd9c026c2a84be4078b6e388a07c00468f5a74a54524b96107`
- `verdict.json` SHA-256:
  `6d1bf3ce6f45f1d9c605806293356370d0e40326040fa875c99df669f75a205e`
- `seed.reveal.json` SHA-256:
  `521dffb97df700b639116147544c3f92b3d5c91ebb0dbb719b5dc5b1a571974b`

The replacement seed material is revealed in its local record.

## Consequence

The earlier `exploratory_lead` now has a fresh exploratory replication on a
larger, balanced consequence-join packet. Promotion remains a separate choice,
but the lab should not respond by rebuilding the procedural trap that almost
prevented this observation.
