# Tests

Tests establish implementation and wire invariants. They do not upgrade mock or
authored behavior into memory evidence.

Run the narrow target named by the nearest subsystem README or specification,
then expand verification in proportion to the change. Tests that preserve a
historical scorer protect its result shape; they do not create a replication.

